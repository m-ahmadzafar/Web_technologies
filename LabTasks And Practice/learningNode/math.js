const add = (a,b) => a + b;
const subtract = (a,b) => a - b;
const multiply = (a,b) => a * b;
const divide = (a,b) => a / b;


//module.exports = {add, subtract, multiply, divide};
//here we export everything we want.


//we can also do it like
exports.remainder = (a,b) => a % b; 